apiVersion: v1
kind: Service
metadata:
  labels:
    app: order-service
    tier: backend
    run: order-service
    visualize: "true"
  name: order-service
spec:
  ports:
  - port: 9004
    protocol: TCP
    targetPort: 9004
  selector:
    app: order-service
    tier: backend
