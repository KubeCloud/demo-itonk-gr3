apiVersion: v1
kind: Service
metadata:
  labels:
    app: product-service
    tier: backend
    run: product-service
    visualize: "true"
  name: product-service
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: product-service
    tier: backend
